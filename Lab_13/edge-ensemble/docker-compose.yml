services:
  onnx_worker:
    build: ./onnx_service
    ports:
      - "8001:8001"

  litert_coordinator:
    build: ./litert_service
    ports:
      - "8000:8000"
    environment:
      - ONNX_SERVICE_URL=http://onnx_worker:8001/predict
    depends_on:
      - onnx_worker